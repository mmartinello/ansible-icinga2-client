TODO:

- support for icinga2_client_vars empty list element, for example:

  barman:
    "Barman Backups":

- Per ogni host che si aggiunge, creare un file importato
  'hostname-custom.conf' non gestito da Ansible (controllare solo che
  esista)
